import Vue from 'vue'
import Component from 'vue-class-component'
//import { Emit, Inject, Model, Prop, Provide, Watch } from 'vue-property-decorator'



@Component({
    components: {
      
    },
    props: {
    }
})

export default class HorizontalMenuComponent extends Vue {
  name: string = 'HorizontalMenuComponent'

  created() {
  }
  mounted() {
  }  
}

