.row
  .col.header
    //- router-link(to="/", tag="div", disabled="true") Test Router
    //- router-link(to="/dashboard", tag="span") dashboard
    //- router-link(to="/signin" tag="li") signIn
    .test(@click="toogleDisable") Toogle Disable
    LinkElement(to="/signup", tag="p" :disabled="dis") signUp
    slot
